// Spot possible bug or bugs in the following code:

let numbers = [42, 0, -1, 100, 9];
let largest = math.max(numbers);

// prints the largest number (100) from the above array in the console:
console.log({ laregst });
